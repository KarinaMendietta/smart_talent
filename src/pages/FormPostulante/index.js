import React from "react";
import { FormControl, InputLabel, Input, FormHelperText } from "@mui/material";

const FormPostulante = () => { 
    return (
        <h1> form del postulante </h1>

    );
};

export default FormPostulante;
